<div id="<%= dom_id category %>">
  <p>
    <strong><%= t('categories.name') %>:</strong>
    <%= category.name %>
  </p>

  <p>
    <strong><%= t('categories.description') %>:</strong>
    <%= category.description %>
  </p>

</div>
